# Dependencies — never commit, too large, auto-installed by npm install
node_modules/
# Environment variables — NEVER commit secrets to GitHub
.env
.env.local
.env.production
# .env.example
# TypeScript build output — generated from src/, not source code
dist/
build/
# OS files
.DS_Store
Thumbs.db
# Logs
*.log
npm-debug.log*
# Test coverage reports
coverage/